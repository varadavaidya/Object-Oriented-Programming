import math

class Circle(object):
    def __init__(self,radius):
        self.radius = radius
    def Area(self):
        return (math.pi)*(self.radius)**2
    def perimeter(self):
        return (2*(math.pi)*self.radius)
a=Circle(4)
print(a.Area())
print(a.perimeter()) 
